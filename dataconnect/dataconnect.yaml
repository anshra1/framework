specVersion: "v1beta"
serviceId: "framework"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "framework-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
